# Root-level example.
# Each app should also have its own `.env.example` with app-specific vars.

# Shared
NODE_ENV=development

# Redis (used by ai-runtime workers, and optionally web)
REDIS_URL=redis://localhost:6379
