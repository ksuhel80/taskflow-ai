"""
Intentionally empty starter module.

Feature code is not generated yet; this file exists only to anchor tooling and imports
for the dev server command in `apps/ai-runtime/package.json`.
"""

from fastapi import FastAPI

app = FastAPI()

